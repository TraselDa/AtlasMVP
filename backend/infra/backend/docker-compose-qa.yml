name: atlas
services:
  backend-qa:
    build:
      context: ../../
      dockerfile: Dockerfile
    container_name: atlas-backend-qa
    env_file: ../env/.backend.qa.env
    environment:
      - WATCHFILES_FORCE_POLLING=true

    ports:
      - "8020:8020"
    volumes:
      - ../../:/app
      - /app/__pycache__
    restart: unless-stopped
    networks:
      - atlas-net

networks:
  atlas-net:
    external: true
