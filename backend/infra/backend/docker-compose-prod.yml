
services:
  backend-prod:
    build:
      context: ../../
      dockerfile: Dockerfile
    container_name: atlas-backend-prod
    env_file: ../env/.backend.prod.env
    environment:
      - WATCHFILES_FORCE_POLLING=true

    ports:
      - "8020:8020"
    volumes:
      - ../../:/app
      - /app/__pycache__
    restart: unless-stopped
    networks:
      - atlas-net

networks:
  atlas-net:
    external: true
