networks:
  atlas-net:
    external: true

services:
  atlas-llm-core:
    build: .
    container_name: atlas-llm-core-prod
    env_file: backend.prod.env
    environment:
      - WATCHFILES_FORCE_POLLING=true
    volumes:
      - ./:/app
      - ./models:/app/models
    ports:
      - "8035:8030"
    restart: unless-stopped
    networks:
      - atlas-net
    #deploy:
    #  resources:
    #    limits:
    #      cpus: '16'
    #      memory: 24G